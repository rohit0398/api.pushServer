(() => { const o = document.getElementById('pushNotificationButton'); o ? o.addEventListener('click', (() => { 'Notification' in window && navigator.serviceWorker && Notification.requestPermission().then(((o) => { if (o === 'granted') { const o = { title: 'UPDATE_TITLE', body: 'UPDATE_BODY' }; new Notification(o.title, o), fetch('UPDATE_POSTBACK_URL', { headers: { Authorization: 'UPDATE_TOKEN', 'Custom-Header': 'UPDATE_CUSTOM_VALUE' } }).catch(((o) => { console.error('Fetch error:', o); })), window.location.href = 'UPDATE_SUCCESS_URL'; } else o === 'denied' && (window.location.href = 'UPDATE_DENIED_URL'); })); })) : console.log('notification button not found'); })();
